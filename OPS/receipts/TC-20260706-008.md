# TruthCal Receipt TC-20260706-008

Date: 2026-07-06
Product: global
Lane: studio/bridge-control-plane
Author: claude-heartbeat
Intent: Stale lock recovery + landing stranded bridge writes (recovery receipt required by BRIDGE_PROTOCOL.md lock rule)
Result: COMPLETE

## What happened

Two stale locks were blocking every write path since last night:

- `.git/index.lock` (created 2026-07-05 21:10, zero bytes, no git process running) — this is why at least two prior heartbeats FAILED to commit despite one claiming "committing them this run". TODAY.md, BRIDGE.json, and INDEX.md sat uncommitted while GitHub readers saw stale state.
- `OPS/BRIDGE.lock` (pid 3, created 2026-07-06T03:10:25Z, dead sandbox process) — tripped the `no active write lock` check in `npm run check`.

Recovery per protocol: verified no writer running, inspected lock metadata, removed both stale locks, reran `npm run bridge:doctor`, wrote this receipt.

Also repaired this run:

- `shared.latest_receipt` pointed at `OPS/receipts/TC-20260706-004.md`, which no longer exists on disk (renumbered to `TC-20260706-007-claude-cowork.md`; that file's internal header says TC-20260706-006 — filename/header mismatch — and its Evidence section is unfilled boilerplate). Pointer repaired; correction line appended to INDEX.md; history not edited.
- BRIDGE.json revision 46 → 47, session_note and sync_notes updated with the integrity flag about the falsely claimed commit.

## Truth

VERIFIED: both lock files existed with the timestamps above; no git or node bridge process was running; git log shows no commit after 2026-07-05 19:25:46 before this recovery; TC-20260706-004.md absent from OPS/receipts/; API_KEY_VAULT.md is gitignored and NOT tracked by git.
VERIFIED: after lock removal, `npm run bridge:doctor` passes all checks except `OPS secret scan clean` (see below).
KNOWN FAIL (pre-existing, surfaced not fixed): `OPS/API_KEY_VAULT.md` (dated Jun 19) contains secret-shaped values inside OPS/, so the bridge secret scan fails. The file is gitignored and untracked — nothing secret is committed or pushed — but the vault living inside the scanned OPS/ tree guarantees a permanent scan FAIL. Ro decision needed: relocate the vault outside OPS/ or whitelist it in scanSecretFiles.
UNKNOWN: whether the 2026-07-06 03:10 heartbeat crashed mid-write (it left BRIDGE.lock behind) — its BRIDGE.json write did land; only its commit did not.

## Evidence

- `git log -1 --format='%ci %s'` before recovery → `2026-07-05 19:25:46 -0700 Merge remote-tracking branch 'origin/main'`
- `git status --short` before recovery → `M OPS/BRIDGE.json · M OPS/TODAY.md · M OPS/receipts/INDEX.md` (plus unrelated untracked files, left alone)
- `OPS/BRIDGE.lock` content → `{"pid":3,"created_at":"2026-07-06T03:10:25.828Z"}`
- `node scanSecretFiles()` → sole finding `OPS/API_KEY_VAULT.md`; `git check-ignore` confirms ignored at `.gitignore:52`
- Commit hash of this recovery: see `git log` entry `ops(heartbeat): stale lock recovery + land stranded bridge writes` on 2026-07-06

## Rollback

`git revert` the recovery commit. Locks must NOT be restored.

## Next action

Codex next boot: execute the active amina/chat-loop-proof lane (one authenticated browser exchange, verify exact persisted rows). Ro: decide on API_KEY_VAULT.md location and rotate OPENAI_API_KEY.
