# TruthCal Receipt TC-20260707-HB-03

Date: 2026-07-07
Product: global
Lane: studio/bridge-control-plane
Author: claude
Intent: Heartbeat recovery — clear stale git lock and corrupt index, release hung Jul-6 commit, root-cause the doctor secret scan failure
Result: COMPLETE

## Truth

VERIFIED: `.git/index.lock` was stale (created 2026-07-05 21:10, 0 bytes, no live git process) and `.git/index` was truncated ("index file smaller than expected"). Both blocked every git write since Jul 5 night — the real cause of the recurring "uncommitted-write drift". Lock removed, index rebuilt with `git reset` on the host; working tree untouched.
VERIFIED: Releasing the lock let a hung commit complete as `4155e93` (2026-07-07 09:56:11). Its diff against parent `84b26cd` is EMPTY — zero content change. Message is stale Jul-6 heartbeat text; misleading but harmless.
VERIFIED: `npm run bridge:doctor` — 8/9 PASS. Single FAIL: OPS secret scan.
VERIFIED: Scan cause is `OPS/API_KEY_VAULT.md` on local disk holding live credential values for roughly 30 providers. The file is untracked AND git-ignored — it cannot be committed as-is.
VERIFIED: The committed and pushed version of that path (commit `7193bd9`) is POINTERS ONLY. No credential values exist in GitHub history. An earlier within-run hypothesis of pushed exposure was checked and is WRONG.
VERIFIED: `npm run check` reported BLOCKER "updated_by is invalid" at rev 85 (a live Cowork write used a non-whitelisted actor string); cured by this write with a valid actor.
WARNING: A handful of values (Notion, Resend, v0) surfaced in a Cowork chat transcript during diagnosis on 2026-07-07 — same exposure class as the 2026-07-04 OpenAI key. Rotation of those three providers is recommended.
UNKNOWN: Whether any agent or engine has copied values from the local vault file elsewhere.

## Evidence

1. `.git/index.lock` stat: 2026-07-05 21:10:49, size 0; `ps` showed no live git commit/merge/checkout process.
2. Pre-repair failure: `fatal: .git/index: index file smaller than expected`.
3. `git diff 84b26cd..4155e93 --stat` → empty output.
4. `npm run bridge:doctor`: FAIL only on "OPS secret scan clean".
5. `git ls-files --error-unmatch OPS/API_KEY_VAULT.md` → not tracked; `git check-ignore` → IGNORED; `git cat-file -e origin/main:OPS/API_KEY_VAULT.md` → absent.
6. `git show 7193bd9:OPS/API_KEY_VAULT.md` → "Status: POINTERS ONLY", no values.

## Next action

Ro: move the values in `OPS/API_KEY_VAULT.md` out of the repo tree (password manager, Keychain, or env files outside the workspace) and restore the pointer-only file so the doctor secret scan passes; rotate Notion, Resend, and v0 keys; refresh `OPS/TODAY.md` to today.
