# TruthCal Receipt — TC-20260705-006

**Timestamp:** 2026-07-05 ~19:10 local (2026-07-06 02:04 UTC merge)
**Actor:** Claude (Cowork session, Ro directing)
**Product:** Amina — iOS Push Build Day 4
**Lane:** amina/ios-submission-qa

## Action attempted
Take over live site, eliminate hydration errors, confirm web surface ready for iOS App Store submission (Capacitor shell loads the live Vercel deployment directly).

## Inputs used
- Live site console inspection (Chrome MCP) on landing + /auth routes
- origin/main at 41ec9c8 (post #47–#51 iOS work)
- v0 branch diff: v0/redlanternstudios-997abb7a vs main
- coordination/IOS_SUBMISSION_HANDOFF.md

## Root cause (VERIFIED)
Pre-paint theme script in `app/layout.tsx` adds `.dark` to `<html>` before React hydrates; `<html>` had no `suppressHydrationWarning`. Every user with stored dark theme (Ro) got a hydration error on every page. Fresh visitors did not — why it looked intermittent.

## Fix shipped
- PR #52, squash-merged to main as `1ddc42b`: `suppressHydrationWarning` on `<html>` + explanatory comment. Keeps the no-flash dark mode behavior (correct long-term pattern, next-themes standard).
- Vercel production deploy `dpl_F5J3SvBMkoZ73Tt8zQksPVCSD7bX` state READY.

## Verification observed
- Set `amina-theme=dark` in localStorage on production, hard-navigated landing + /auth: `.dark` applied pre-paint, ZERO console errors. The exact pre-fix failing condition now passes. Test key removed after.

## Risk flagged (do NOT merge)
`v0/redlanternstudios-997abb7a` regresses two decided things:
1. Deletes the theme script (reintroduces dark-mode FOUC) instead of the suppressHydrationWarning fix.
2. Re-swaps chat to `openai/gpt-4-turbo` + invalid `maxTokens` — regressing the owner-decision gpt-4o setup from #46. Matches the standing handoff rule: no v0/* merges in the submission window without `pnpm test:invariants`.
Cosmetic marketing-page class refactors in that branch can be cherry-picked later if wanted; nothing there is needed for submission.

## Remaining to submission (Keymon-owned, per IOS_SUBMISSION_HANDOFF.md)
Apple signing → privacy manifest validation in Xcode → Release archive → TestFlight → authenticated device matrix (incl. RevenueCat sandbox) → submit. Web surface: no known blockers.

## Note
`capacitor.config.ts` still points the shell at `amina-muslima-companion.vercel.app` while prod verification references `myamina.app`. Same project; works either way. UNKNOWN whether myamina.app should be canonical for the shell — decide before archive if deep links/auth callbacks assume the custom domain.
