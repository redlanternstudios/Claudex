# TruthCal Receipt — TC-20260705-004
## Amina Live Site E2E — Day 4 iOS Push

**Timestamp:** 2026-07-05 ~19:20 local
**Actor:** Claude (Cowork session, Ro directing)
**Product:** Amina — live site (amina-muslima-companion.vercel.app, shell target for iOS)
**Prior receipt:** TC-20260705-003 (hydration root cause fix, PR #52)

---

## 1. Repo state (VERIFIED)

| Item | State |
|---|---|
| main HEAD | `756c75f` — all local work committed and pushed |
| Hydration fix | `1ddc42b` (PR #52) on main, deployed |
| Working tree | clean, in sync with origin/main |
| Outstanding files | 8 spec/dispatch docs committed in `756c75f` (2,231 lines) |
| v0 branch | `v0/redlanternstudios-997abb7a` UNMERGED — must stay unmerged (chat model + FOUC regressions, see TC-20260705-003) |

## 2. Production deploy (VERIFIED)

- Vercel project `amina-muslima-companion` (prj_dhX6k7fBmSKMc3Ts86bLctZPT9sZ)
- Deploy `dpl_F5J3SvBMkoZ73Tt8zQksPVCSD7bX` — state READY, target production, commit 1ddc42b
- A newer deploy will follow `756c75f` (docs only, zero runtime change)

## 3. E2E — unauthenticated surface (VERIFIED, evidence attached)

Method: live Chrome session per route — hard navigation, 2 to 3s settle, full console read (all levels), screenshot. Separate headless Chrome captures saved as evidence files.

| Route | Renders | Console | Evidence |
|---|---|---|---|
| `/` (landing) | PASS | clean, zero messages | `TC-20260705-004-evidence/web-landing.png` |
| `/welcome` (signup carousel) | PASS | clean | `web-welcome.png` |
| `/auth` (sign in) | PASS | clean | `web-auth.png` |
| `/privacy` | PASS — OpenAI processor disclosure present | clean | `web-privacy.png` |
| `/terms` | PASS (see finding F1) | clean | `web-terms.png` |
| `/community-standards` | PASS | clean | `web-standards.png` |
| Auth gate | `/home` unauthenticated → redirects to `/auth?redirect=%2Fhome` | correct behavior | — |

**Hydration regression check (VERIFIED):** with `amina-theme=dark` in localStorage on production, `.dark` applied pre-paint and console stayed clean on `/` and `/auth` — the exact pre-fix failing condition now passes. Test key removed after.

## 4. E2E — authenticated surface: PENDING RO SIGN IN

I cannot enter passwords (hard rule). Waiting on Ro to sign in to the test account in the live Chrome tab. Planned coverage once session exists: home (dynamic reflection + feed), chat send → streamed gpt-4o reply, circles (browse/join/post), du'a wall + ameen, profile, sign out. Console + screenshots + Supabase side effect checks per step. Results will be appended as TC-20260705-004b.

## 5. Findings

**F1 — Terms vs age gate conflict (NEEDS RO DECISION, pre-submission):** `/terms` says "Amina is available only to people who are at least 18 years old." The code-enforced age gate is 13+ and the handoff doc instructs a 13+ App Store rating. Apple reads the terms. Either terms are correct (then gate + rating must move to 17+/18) or the gate is correct (then terms copy is stale). One line fix either way, but it is an owner call.

**F2 — Shell URL canonical question (carry over from TC-20260705-003):** `capacitor.config.ts` points at `amina-muslima-companion.vercel.app`; prod also answers at `myamina.app`. Decide canonical before archive.

## 6. Verdict

Web surface: **GREEN for everything reachable without auth.** Zero console errors anywhere, hydration fix verified on prod, repo fully committed and pushed. Authed leg pending one manual sign in from Ro. F1 needs an owner decision before Apple submission.
