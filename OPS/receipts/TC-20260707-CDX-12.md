# TC-20260707-CDX-12

Date: 2026-07-07T14:20:46-0700
Product: claudex
Lane: heartbeat-scheduled-task
Author: codex
Status: COMPLETE

## Intent

Install Claudex Heartbeats as a scheduled task running on the same cadence pattern as Rory's setup.

## Evidence

- Added `scripts/heartbeat.mjs`.
- Added `scripts/install-heartbeat-scheduler.mjs`.
- Added `heartbeat` and `heartbeat:install` npm scripts in `package.json`.
- Updated `templates/HEARTBEAT_SCHEDULE_TEMPLATE.md` with the heartbeat install command.
- Installed LaunchAgent `com.redlantern.claudex.heartbeat` at `/Users/kp/Library/LaunchAgents/com.redlantern.claudex.heartbeat.plist`.
- Installed LaunchAgent `com.redlantern.claudex.watch` at `/Users/kp/Library/LaunchAgents/com.redlantern.claudex.watch.plist`.
- `launchctl print gui/501/com.redlantern.claudex.heartbeat` reported `run interval = 3600 seconds`, `runs = 2`, and `last exit code = 0`.
- `launchctl print gui/501/com.redlantern.claudex.watch` reported `run interval = 300 seconds` and `last exit code = 0`.
- `.claudex/heartbeat-last.json` reported `sync_ok: true`, `doctor_ok: true`, `status: YELLOW`, and `digest_path: /Users/kp/Penn Enterprises LLC/Claudex/.claudex/heartbeat-last.md`.

## Result

COMPLETE: local Claudex heartbeat schedule is installed and running hourly; local Claudex watcher is installed and running every five minutes. GitHub alignment remains on its existing fifteen-minute workflow.

## Truth Boundary

VERIFIED: The local macOS scheduled tasks are installed and have run.

VERIFIED: The heartbeat runner completed successfully after patching it to avoid launchd PATH dependence.

PARTIAL: The heartbeat digest reports `SYNC YELLOW` because four open questions remain in `OPS/questions/`. That is an existing bridge-state warning, not an installation failure.

## Undo

Run:

```bash
launchctl bootout gui/$(id -u) /Users/kp/Library/LaunchAgents/com.redlantern.claudex.heartbeat.plist
launchctl bootout gui/$(id -u) /Users/kp/Library/LaunchAgents/com.redlantern.claudex.watch.plist
```

Then remove the two plist files if the scheduled tasks should not return.
