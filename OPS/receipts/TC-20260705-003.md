# TruthCal Receipt TC 20260705 003

Date: 2026 07 05
Product: amina
Lane: amina/ios submission qa
Author: claude
Reviewer: Keymon
Intent: Repair and verify iPhone safe areas (acceptance check 4 of TC-20260705-001, the receipt's "first action")
Result: FIXED AND VERIFIED

## Reality check

VERIFIED: Root cause — no safe-area handling existed anywhere: viewport lacked viewport-fit=cover (env(safe-area-inset-*) always 0 in WKWebView) and BottomNav referenced a pb-safe class that was never defined.

FIXED (Amina PR #48, main `caa7c90`): viewportFit 'cover'; .pb-safe/.pt-safe utilities defined; additive safe-area top padding on marketing nav, sticky AppHeader, (auth) layout wrapper, chat header via inline calc()/max(). Desktop web pixel-identical (insets 0).

VERIFIED: Post-deploy re-capture on the same booted iPhone 17 Pro simulator (app relaunched; shell loads the remote Vercel deployment, so no new archive was needed): status bar, Dynamic Island, and home indicator all clear; app chrome renders fully below/above system UI. Evidence: /tmp/amina-safe-fixed.png (compare Codex's /tmp/amina-after-wait.png).

NOTE: simctl required explicit DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer (machine xcode-select still points at CommandLineTools — Keymon may want to run sudo xcode-select -s /Applications/Xcode.app).

## Acceptance delta vs TC-20260705-001

Check 4 (first screen respects iPhone safe areas): FAIL → PASS (simulator-verified).
Check 6 (privacy disclosure matches implementation): FAIL → PASS (TC-20260705-002).
Check 7 (privacy manifest present): FAIL → PRESENT, validation pending Keymon (TC-20260705-002).
Remaining FAIL/NOT RUN: 5 (signing), 8-11 (authenticated matrix, purchases, deletion, physical device) — all Apple-account or device gated, owned by Keymon.

## Next action

Keymon: Apple signing → validate PrivacyInfo.xcprivacy + App Privacy (13+) → Release archive → TestFlight → authenticated device matrix → submit.
