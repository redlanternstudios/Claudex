# TruthCal Receipt TC 20260706 005

Date: 2026 07 06
Product: amina
Lane: amina/notification-system
Author: claude
Intent: Implement the Amina Notification System Build Packet v3 on the real stack
Result: SHIPPED (device verification of delivery = Keymon, after cap sync)

## Reality check

VERIFIED (Amina PR #60 `ca76eba`, migration 20260706_reminder_time):
- Packet adapted honestly: it assumed Expo/React Native; Amina is Next.js + Capacitor. Same system,
  real stack.
- Content law as CODE: 6 intents w/ approved copy, every CTA deep-links, forbidden-language gate
  (guilt/fear/urgency/fatwa/shame/scoring/streak-pressure) enforced by pnpm test:notifications (8/8).
  Reconciliation: packet's 54-char body minimum contradicts its own approved samples (42-53) —
  samples win, floor 40.
- Tokens §04 verbatim; surfaces §03: AminaNotificationCard (356px, 36px seal, safety variant),
  AminaInAppToast (safe-area, translucent, reduce-motion), AminaJourneySheet (dismissible).
- REAL delivery: @capacitor/local-notifications@8 — daily reminder scheduled ON DEVICE at
  amina_profiles.reminder_time (user-chosen, default 20:00). One/day, approved copy only, no server
  push, tap deep-links (handler in app layout). Web reports unsupported and the Profile UI says so —
  no dead toggles (audit rule honored).
- First live usage: circle ShareCard window.alert (audit-flagged dead pattern) → branded toast.

## Keymon native step (added to iOS lane)

npx cap sync ios before the Release archive so the LocalNotifications pod ships; then device-verify:
enable reminders → iOS permission prompt → notification fires at chosen time → tap opens /reflections.

## Notes

Bell/notification center remains intentionally absent until a real inbox exists (audit §3.2).
