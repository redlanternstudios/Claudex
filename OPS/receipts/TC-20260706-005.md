# TruthCal Receipt TC-20260706-005

Date: 2026-07-06
Product: amina
Lane: amina/ios-submission-qa
Author: codex
Intent: Amina trust flows hardened after trust closure
Result: PARTIAL

## Truth

VERIFIED: Receipt created through the Claudex command layer.
VERIFIED: Amina branch `feat/trust-closure` was pushed with trust hardening commit `b1b9865`.
VERIFIED: Local Amina checks passed after the hardening patch.
VERIFIED: The patch is local first. No n8n workflow was introduced.
VERIFIED: XcodeBuildMCP defaults are set for the Amina iOS App scheme and simulator.
BLOCKED: Local simulator launch is blocked because `xcode-select` points to CommandLineTools and `simctl` is unavailable.
BLOCKED: Switching `xcode-select` to full Xcode requires a sudo password in this Codex session.
UNKNOWN: Live App Store Connect, TestFlight, physical device, and live Supabase schema state were not verified by Codex in this receipt.

## Evidence

Repository:

1. Amina commit `b1b9865` on `feat/trust-closure`.
2. Amina receipt `OPS/receipts/trust-flow-hardening-2026-07-06.md`.

Code changes:

1. Memory resurfacing previews without spending cooldown.
2. Memory cooldown is acknowledged only after Reflect or Let it rest.
3. Home more button opens conversation history.
4. Chat more button opens history and archive actions.
5. Memory deletion in Profile rolls back visibly on failure.
6. Keep insight verifies conversation ownership.
7. Keep insight avoids silent partial save when memory consent fails.
8. Export fails safely instead of returning incomplete data.
9. RevenueCat cancellation and billing issue preserve access through a known paid period.
10. RevenueCat expiration, refund, and revoke remove access immediately.
11. Deletion cascade migration is replay safe.

Checks:

1. `pnpm type-check` passed.
2. `pnpm test:invariants` passed with 11 tests.
3. `pnpm build` passed with all 58 static pages generated.
4. `git diff --check` passed.
5. XcodeBuildMCP `session_show_defaults` showed Amina App defaults.
6. XcodeBuildMCP `build_run_sim` failed before build because `simctl` is unavailable.
7. `xcode-select -p` returned `/Library/Developer/CommandLineTools`.

## Next action

Human or Keymon: run `sudo xcode-select -s /Applications/Xcode.app`, then rerun the simulator capture.

Keymon: select the Apple development team, create the signed Release archive, upload to TestFlight, complete the authenticated device matrix, then submit.
