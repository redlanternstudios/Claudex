# TruthCal Receipt TC-20260707-008

Date: 2026-07-07
Product: claudex
Lane: claudex/os
Author: codex
Intent: Fix bridge publish post-push stdio crash
Result: COMPLETE

## Truth

VERIFIED: Receipt created through the Claudex command layer.
VERIFIED: `npm run bridge:publish` successfully pushed `50b112e` to `origin/main`.
VERIFIED: After the push, the command crashed in `scripts/alignment.mjs` because `execFileSync` returns `null` when `stdio: 'inherit'` is used and the helper called `.trim()` on that null value.
VERIFIED: `scripts/alignment.mjs` now treats non-string Git output as an empty string, preserving inherited-stdio push behavior without crashing.
VERIFIED: Before this fix, `npm run bridge:sync` reported local and remote aligned at `TC-20260707-007` with only this script change dirty.

## Evidence

- Failed command: `npm run bridge:publish`
- Push confirmation before crash: `ababa3e..50b112e HEAD -> main`
- Error: `TypeError: Cannot read properties of null (reading 'trim')`
- Fix: `scripts/alignment.mjs` stores the `execFileSync` result and returns `output.trim()` only when `typeof output === 'string'`; otherwise it returns an empty string.

## Next action

Run `npm run check`, commit this fix, and rerun `npm run bridge:publish`.
